rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /certificates/{allPaths=**} {
      allow read: if true;    // descarga pública
      allow write: if request.auth != null && request.auth.token.email == 'the.piece.maker@gmail.com';
    }
  }
}
